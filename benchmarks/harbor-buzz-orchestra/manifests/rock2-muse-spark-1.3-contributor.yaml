# rock2 bake-off, Muse Spark leg. One agent, the production base prompt from the
# checked-out source build, and the same persona byte-pin the buzz-native solo
# conditions use — so this condition differs from those in exactly one axis:
# the ACP adapter under buzz-acp, and therefore the model that answers.
#
# The adapter is deployment config, not manifest: the endpoint config
# names it with agent_command, agent_args and agent_binary.
schema_version: "1"
condition: rock2-rebrand-acp-muse-spark-1.3-contributor
roster:
  - id: solo
    kind: orchestrator
    role: solo
    count: 1
    endpoint: muse-spark-1.3-contributor
    model_revision: muse-spark-1.3-contributor
    prompt:
      path: personas/buzz-native-solo.md
      sha256: 972950f0e2bfb9bf540c98e70e075479ab80cd596cb5ad405dad0cafdc60840b
    generation:
      max_output_tokens: 8192
      context_window_tokens: 200000
      thinking_effort: medium
prices:
  # Read from of::pricing at aitaco-llc/rebrand#306 (neil, 2026-09-21).
  # Output includes thinking tokens at one rate.
  muse-spark-1.3-contributor:
    input_per_million_usd: 0.10
    cached_input_per_million_usd: 0.002
    output_per_million_usd: 0.20
trial_budget:
  timeout_seconds: 300
